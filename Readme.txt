This App is a simple PDF Reader that accepts a PDF as an input,
It then uses pyPDF2 module to read files from the PDF and extracts a clean text from it,
Then uses the pyttsx3 module to convert this text to speech using an instace of the speaker engine.